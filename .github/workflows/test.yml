name: Testing deployment

on:
  push:

jobs:
  unit-test:
    name: run unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: test
        run: python -m unittest